# Oh-My-Zsh aliases g to git
unalias g
# Run wrapper first if it is present
function g() {
  if [ -f ./gradlew ]; then
    ./gradlew "$@"
  else
    echo "WARNING: Using installed Gradle binary over wrapper"
    command gradle "$@"
  fi
}
alias gradle="g"
