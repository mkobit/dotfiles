# Python tooling rules for building and deploying CLI tools

load("@rules_python//python:defs.bzl", "py_binary")

package(default_visibility = ["//visibility:public"])

py_binary(
    name = "ty",
    srcs = ["ty_wrapper.py"],
    main = "ty_wrapper.py",
    deps = ["@pypi//ty:pkg"],
    data = ["@@rules_python++pip+pypi_314_ty//:bin/ty"],
)
