load("@pypi//:requirements.bzl", "requirement")
load("@rules_python//python:defs.bzl", "py_binary")

# Generate tool-specific rule files from canonical .rules/ sources.
# Run with: bazel run //tools/rules:generate
py_binary(
    name = "generate",
    srcs = ["generate.py"],
    main = "generate.py",
    deps = [requirement("python-frontmatter")],
)
