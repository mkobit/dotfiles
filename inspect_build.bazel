load("@rules_python//python:defs.bzl", "py_binary")
load("@pypi//:requirements.bzl", "requirement")

py_binary(
    name = "inspect_whisper",
    srcs = ["inspect_whisper.py"],
    deps = [
        requirement("openai-whisper"),
    ],
)
