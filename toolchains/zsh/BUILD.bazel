package(default_visibility = ["//visibility:public"])

# Toolchain type for zsh
toolchain_type(
    name = "toolchain_type",
    visibility = ["//visibility:public"],
)

# Export the toolchain files
exports_files([
    "toolchain.bzl",
    "extensions.bzl",
])
