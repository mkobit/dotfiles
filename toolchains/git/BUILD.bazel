package(default_visibility = ["//visibility:public"])

# Toolchain type for git
toolchain_type(
    name = "toolchain_type",
    visibility = ["//visibility:public"],
)

# Export the toolchain files
exports_files([
    "toolchain.bzl",
    "mock_toolchain.bzl",
    "extensions.bzl",
])
