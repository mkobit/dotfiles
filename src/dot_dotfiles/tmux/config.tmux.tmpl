{{/* Tmux configuration managed by chezmoi */}}
{{/* Auto-sources all .tmux files in snippets directory */}}

# Source all snippet files using tmux 3.2+ format expansion and glob patterns
source-file -F "#{d:current_file}/snippets/*.tmux"