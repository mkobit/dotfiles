{{/* Tmux configuration managed by chezmoi */}}
{{/* Auto-sources all .tmux files in snippets directory */}}

# Source all snippet files
{{- $targetDir := dir .chezmoi.targetFile }}
run-shell 'for file in {{ $targetDir }}/snippets/*.tmux; do [[ -r "$file" ]] && tmux source-file "$file"; done'