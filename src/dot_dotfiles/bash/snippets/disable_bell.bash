# Disable terminal bell/beep sounds in bash
# This prevents audio notifications from readline and bash completion

# Disable readline bell (bash completion, history, etc.)
bind 'set bell-style none' 2>/dev/null || true