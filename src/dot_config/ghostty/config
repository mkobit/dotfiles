# Ghostty Terminal Configuration
# See: https://ghostty.org/docs/config/reference

# Include modular configuration files
config-file = conf.d/bell.conf
config-file = conf.d/claude-ide-shift-enter.conf
config-file = conf.d/font.conf
config-file = conf.d/macos.conf
config-file = conf.d/interaction.conf