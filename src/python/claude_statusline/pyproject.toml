[project]
name = "claude-statusline"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
    "rich>=15.0.0",
    "typer>=0.15.1",
    "whenever>=0.9.5",
]

[project.scripts]
claude-statusline = "claude_statusline.main:cli"

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.15.7",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
