[project]
name = "termtext"
version = "0.0.0"
description = "Terminal text formatting library"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
