[project]
name = "tmux-url-open"
version = "0.1.0"
description = "Extract and open URLs from a tmux pane"
authors = [{ name = "Jules", email = "jules@example.com" }]
requires-python = ">=3.11"
dependencies = ["click>=8.1.8"]

[project.scripts]
tmux-url-open = "tmux_url_open:main"
