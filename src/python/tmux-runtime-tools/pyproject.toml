[project]
name = "tmux-runtime-tools"
version = "0.1.0"
description = "Runtime tools for tmux environment"
authors = [{ name = "Jules", email = "jules@example.com" }]
requires-python = ">=3.14"
dependencies = ["click>=8.1.8"]

[project.scripts]
tmux-runtime-tools = "tmux_runtime_tools:main"
