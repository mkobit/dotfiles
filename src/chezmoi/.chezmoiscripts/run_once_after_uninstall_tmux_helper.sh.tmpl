#!/usr/bin/env bash

set -euo pipefail

echo "Cleaning up legacy tmux python tools..."

{{ template "uv/tool.sh.tmpl" (dict "action" "uninstall" "name" "tmux-helper") }}
{{ template "uv/tool.sh.tmpl" (dict "action" "uninstall" "name" "tmux-runtime-tools") }}
