# Files to remove from target system
# https://www.chezmoi.io/reference/special-files/chezmoiremove/

# Old asdf install script (renamed to configure-asdf)
install-asdf.sh

# Custom tmux-powerline theme (using default theme with config overrides instead)
.config/tmux-powerline/themes/dotfiles.sh

# Remove asdf binary and configuration
.asdf
.local/bin/asdf

# Unused chezmoi config file (chezmoi only reads from ~/.config/chezmoi/chezmoi.toml)
# This was accidentally placed in source directory and never actually used
.chezmoi.toml

# Old Claude Status Line files
.local/bin/claude_statusline.py
.local/bin/test_claude_statusline.py

# Old hello_world example
.local/bin/tools/hello_world

# Superseded by upstream skill-creator (deployed via chezmoi externals)
.claude/skills/claude-extension-builder
.claude/commands/claude/new/skill.md

# Remove Oh-My-Zsh installation and custom configuration files when it is disabled
{{ if not (index .zsh "oh-my-zsh").enabled }}
.dotfiles/external/oh-my-zsh
{{ end }}
