# Ghostty Bell Configuration - COMPLETELY DISABLED
# See: https://ghostty.org/docs/config/reference#bell-features

# Disable all bell features to prevent any audio or visual notifications
# Features prefixed with 'no-' are explicitly disabled:
#   - no-attention: Don't request user attention when unfocused
#   - no-title: Don't prepend bell emoji (🔔) to window title
#   - no-system: Don't use system bell notifications
#   - no-audio: Don't play custom sounds
#   - no-border: Don't display border around alerted surface
bell-features = no-attention,no-title,no-system,no-audio,no-border